/**
 * there are 3 types of error
 * compile time error
  *   those which is going to be recognized by the compiler itself
 *    ex-System.out.PRint
 * 
 * runtime error
 *   execution will stop
 *   those which are identified as error during runtime
 *   ex-divide by zero
 * 
 *   they are also known as exception
 *   we have deal with that
 * 
 * logical error
 */